# v0.3.3 Release Closeout

Date: 2026-06-12

## Scope

Prepare and publish `v0.3.3`, covering post-`v0.3.2` helper changes:

- package-aware ledger/status visibility;
- human-readable At a Glance status output;
- missed heartbeat detection and macOS watchdog fallback/status;
- package-level external review workflow;
- release helper stable/prerelease flag correction.

## Pre-Release Verification

- `go test ./...`
- `bash -n scripts/build-release-assets.sh scripts/publish-release.sh scripts/install-macos-watchdog.sh scripts/macos-watchdog-run.sh`
- `git diff --check`
- `go run ./cmd/codex-orchestrator validate-routines --dir routines --json`
- `go run ./cmd/codex-orchestrator policy check --repo . --write-report /tmp/codex-orchestrator-policy-check.json --json`
- `go run ./cmd/codex-orchestrator run-routine docs-drift-checker --repo . --write-report /tmp/codex-orchestrator-docs-drift.json --json`
- `go run ./cmd/codex-orchestrator run-routine evidence-label-auditor --repo . --write-report /tmp/codex-orchestrator-evidence-label.json --json`
- `scripts/build-release-assets.sh v0.3.3 /tmp/codex-orchestrator-v0.3.3-dist`

## Evidence Labels

- `local`: tests, script syntax checks, release asset build, local tag
  inspection after tagging, and source build smoke.
- `proxy`: GitHub release metadata, GitHub release asset names, and downloaded
  release asset smoke after publishing.
- `blocked`: none before publication; GitHub Actions or GitHub API failures
  will be recorded here if they occur.

## Boundaries

- No Homebrew, npm, tap, registry, daemon, deployment, payment, hardware, or
  production path is in scope.
- Release verification does not claim direct Codex App session-launch proof,
  direct production/runtime proof, or OS wake reliability proof.

## Publication Result

- Published tag: `v0.3.3`
- GitHub Actions run: `27391510855`
- Release URL:
  https://github.com/indiekitai/codex-orchestrator/releases/tag/v0.3.3
- Build matrix passed:
  - `darwin_arm64`
  - `darwin_amd64`
  - `linux_amd64`
  - `linux_arm64`
  - `windows_amd64`
- Publish job passed with `softprops/action-gh-release@v2`.
- `go run ./cmd/codex-orchestrator run-routine release-verifier --tag v0.3.3 --repo . --write-report /tmp/codex-orchestrator-v0.3.3-release-verifier.json --json`
  passed:
  - local tag resolves to annotated tag object at commit
    `b0c0950bc67acfe2bb1a52dab1644ec1c02b4345`;
  - GitHub Release is `draft=false`, `prerelease=false`;
  - expected darwin/linux/windows release asset names are present.
- Download smoke:
  - downloaded `codex-orchestrator_darwin_arm64.tar.gz` from the GitHub Release;
  - extracted `codex-orchestrator_darwin_arm64`;
  - `--help` lists the new `watchdog status` command;
  - `watchdog status --repo . --json` runs and reports `evidenceLabel=local/static`.

## Notes

- GitHub Actions emitted Node.js 20 deprecation and missing `go.sum` cache
  warnings. They did not fail the build or release.
- The release verification uses local and proxy evidence only. It does not claim
  direct production/runtime proof, direct Codex App session-launch proof, or OS
  wake reliability proof.
